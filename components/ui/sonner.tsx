"use client"

/**
 * Pebble toast UI (Groot-inspired stack).
 * Prefer importing from `@/components/ui/pebble-toast`.
 * This file re-exports for existing `@/components/ui/sonner` imports.
 */
export { Toaster } from "@/components/ui/pebble-toast"
