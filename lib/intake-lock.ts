/** Cookie names for client-intake session lock (set by proxy on /submit). */
export const INTAKE_LOCK_COOKIE = "vsop_intake_lock";
export const INTAKE_PORTAL_COOKIE = "vsop_intake_portal";

/** Staff unlock: open /login?staff=1 to clear the intake lock. */
export const INTAKE_STAFF_UNLOCK_PARAM = "staff";
